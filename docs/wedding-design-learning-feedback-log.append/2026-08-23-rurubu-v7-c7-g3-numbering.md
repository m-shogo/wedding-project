# Rurubu V7 design-learning feedback — C7 promotion / G3 rejection

Date: 2026-08-23

This run changed judgment rather than merely styling numbers.

Fresh professional reference work emphasized two simultaneous truths: navigation should reflect actual reader use, and a visual system should remain when it has a valid practical reason. That led to two opposite local decisions.

- G3 `2378:2`: rejected. Removing `01–04` and relying only on `朝 / 昼 / 夕 / 夜` remained readable but reduced the scan rhythm and high-energy travel-guide pacing that the numbers were genuinely carrying. G2 `2299:2` remains current.
- C7 `2379:2`: promoted. The front-cover `01` in C5 had no verified sequence/page/issue referent, so it was changed to the already-authoritative quantity `4` from `ふたりの4つ。`. This retained the visual beat while making the number reader-facing and semantically accountable.

Three-scale evidence exists for both tests. C7 structure after promotion: native text 18, IMAGE 6, text intersections 0, 18 px edge risks 0, Japanese font mismatch 0. Current V7 roots have 0 pairwise overlaps. G3 is hidden rejected evidence; C5 is hidden rollback.

Learning impact: do not transfer a successful subtraction rule blindly between V7 and V8. Ask what the element actually does for the reader. A number can be kept for real scan/navigation/pacing work, or replaced when it merely simulates magazine structure.

Asset truth: generation 0, Drive writes 0, new masters 0, new image hashes 0, final Hawaii photography 0. V6 and V8 unchanged.