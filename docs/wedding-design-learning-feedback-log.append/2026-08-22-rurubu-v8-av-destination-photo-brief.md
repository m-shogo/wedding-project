# Rurubu V8 — Outer AV destination-language reconciliation + photo authenticity brief

Date: 2026-08-22
Scope: Rurubu WEDDING only

## Observed

Live Figma had moved the V8 Outer current root from AP `2251:2` to AV `2273:24`, while GitHub Current Status still named AP. AV changes the support deck from publication inventory language to concrete destination-experience language without changing the layout or image role.

At the same time, V8 still loses to V6 on travel desire because the visible supporting visual is an abstract ocean-light image rather than destination-specific Yokohama photography.

## New professional input

This run rotated into travel-editorial/photo commissioning rather than repeating book-layout references. Useful principles:

- travel expertise and utility can coexist with experimental print photography;
- authenticity is strengthened by local/ordinary/lived-in evidence, not only iconic landmarks;
- walking, overlooked infrastructure and local voices can reveal a place more convincingly than generic destination beauty.

## Bounded changes

1. Re-verified AV at 500px, 1400px and 1587×1123; structure remains clean.
2. Refined existing `OUTER-01` photo-art-direction brief `2270:2` to require lived-in city traces and to target `この街を歩いてみたい`, while preserving crop/text-safe and anti-AI constraints.
3. First expanded brief text overflowed into the next section. It was rejected and tightened before screenshot QA passed.
4. Re-read V8 Drive authority. No new role-specific OUTER-01 master exists, so no production image placement was claimed.

## Learning states

- RSL-222: `VERIFIED_LOCAL → CROSS_ITEM_CANDIDATE`
- RSL-223: `OBSERVED → ROOT_CAUSE_HYPOTHESIS`

## Asset truth

- generated this run: `0`
- new Drive masters: `0`
- new production Figma image placements: `0`
- V6/V7 images reused: `0`

## Next receiving experiment

When a role-specific OUTER-01 master is actually generated, compare multiple materially different candidates for:

- destination specificity without fake text/signage;
- lived-in evidence versus postcard beauty;
- text-safe/crop resilience;
- `行きたい / 歩いてみたい` response at thumbnail and reading scale;
- effective resolution at final use size.

Then close the full lifecycle: generated candidate → selected master → V8 Drive → readback ID → role derivative → verified transport (prefer RSL-221 if official submit remains blocked) → exact Figma role → image hash/crop → three-scale QA.
