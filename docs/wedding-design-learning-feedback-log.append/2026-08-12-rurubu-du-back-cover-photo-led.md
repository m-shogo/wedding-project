## 2026-08-12 — Rurubu DU back-cover photo-led rhythm

### Visible problem
DT had become a strong destination-led front cover, but its back cover still resolved into a dominant image followed by an orderly lower strip. At whole-item scale the two faces did not yet share the same energetic travel-magazine grammar.

### Principle tested
When one face of a print item is already photo-led, judge the entire item from scratch rather than polishing only the strongest face. Rebuild the weaker face from photo scale, overlap, native type, captions, and editorial rhythm before adding containers.

### Experiment / evidence
Rollback-safe DU `992:2` preserved DT's front and rebuilt only back `992:3`. The main photograph became a `793.7 × 620` substrate; two friend photographs became unequal overlapping anchors; existing hidden rectangles became slim square-corner caption strips; and the travel timeline was compressed into a staggered lower rhythm.

The first pass with captions floating below the photographs was rejected at actual size. Captions were attached to the photo edges and the page was rechecked.

Final QA: 500px thumbnail PASS, whole-reading PASS, natural-size back `794 × 1123` PASS; `37` visible native text nodes, `7` IMAGE fills, `0` same-parent text intersections, fold guide `992:184` at `x=792.7000122070312`, `2 × 1122.5`.

### Result
DU was selected over DT for whole-item balance and promoted to Review as `993:2`. DT Review `987:2` is hidden rollback. Start Here now reads `DU outer / DF inside`. Current `77:18 / 77:290` was not changed.

### Regression risk
Photo overlap becomes scrapbook-like when rotation is overused; dark caption strips become UI-like when enlarged or repeated. Use them only when they bind captions to photography and improve actual-size reading.

### Asset-state caution
The DU front hero hash `539c259be8036b481d06b4f76db9a39b407d90e8` is visually verified existing Figma reuse, not exact Q60 provenance. Fresh Q60 Drive master remains ID `1YL0WAOzYU3O1FGa23ieRuw_Btu4jbmzr`, `155439` bytes. A fresh official upload URL was issued but its byte POST was DNS-blocked, so external binary placement remains `NO`.

### Next application
Carry the whole-item scratch-selection test into later magazine work: compare both faces together at thumbnail, then inspect the changed face at actual size. Do not promote asset provenance from visual similarity alone.

Status: `VERIFIED_COMPARATOR_LEARNING / DU_ADOPTED / CURRENT_UNCHANGED / Q60_EXACT_PLACEMENT_OPEN`
