# Rurubu V6 — BQ selective photo-bound chronology feedback

Date: 2026-08-16
Scope: Rurubu WEDDING only

## Observation

BH chronology was structurally correct but still showed a residual split between milestone copy and event photography. The top also repeated the same sunset source used by event 01.

## Hypothesis

A travel-magazine chronology can feel more edited if only image roles that can truly support copy receive photo-bound native text. Uniform treatment is not required. Duplicate source use and non-contributing decoration should be removed first.

## Test and result

A rollback-safe clone progressed through BL/BM/BN/BO/BP/BQ.

- direct copy binding for 01 and 05: adopted;
- direct copy binding for small event 03: rejected and restored outside the photo;
- duplicated upper sunset support: removed;
- separate old-town support: retained/rebalanced;
- reused generated timeline image with no meaningful visible role: hidden;
- all structural collisions/safe-area defects discovered during the test: repaired before promotion.

Three-scale result:

- 500px whole: PASS;
- 1000px reading: PASS;
- actual-size `794×1123`: PASS;
- native text `30`;
- visible replaceable IMAGE roles `5`;
- absolute text collision `0`;
- 18px safe-area risk `0`.

## Adoption

BQ `1468:2` promoted. BH `1451:2` preserved hidden as rollback. Start Here synchronized to `V6 T + BK/BQ`; V7 remains HOLD.

## Asset-state truth

- generated this run: 0;
- Drive saved this run: 0;
- new binary placement: 0;
- existing verified Figma hashes reused: yes;
- generated section decoration newly adopted: no;
- visually verified: yes;
- structure verified: yes.

## Next application

Continue judging T + BK/BQ as one magazine system. The next edit should target a visible system-level defect rather than adding decorative count. Real copy/photo replacement must trigger fresh crop, contrast and dynamic-text checks.
