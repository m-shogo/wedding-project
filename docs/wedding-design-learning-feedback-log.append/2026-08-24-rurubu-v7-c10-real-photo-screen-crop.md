# Rurubu V7 C10 real-photo screen-crop feedback

Date: 2026-08-24

New professional knowledge changed the design decision in this pass. Instead of treating the first real-photo placement as automatically better because it was legitimate, the photo was reviewed as an editorial image whose crop must balance the human subject and destination context.

C9's wider screen crop made Hawaii clear but left the couple relatively small. C10 used the same legitimate source and changed only crop, producing a stronger wedding + Hawaii first read at 500px and 1400px.

The pass also made the completion boundary more explicit: the Figma derivative is only `350×233`, while the Drive master is `4500×3000`. Therefore C10 is preferred **screen-composition evidence**, not actual-size evidence and not a current/final cover. C8 remains current until the full-resolution master can be placed and independently QA'd.

A fresh high-resolution placement attempt was justified only because the Drive master became locally materializable. The final `mcp.figma.com` upload still failed at DNS, so the same route was not repeated.

Before/after learning check: **YES**. The new photo-edit/crop research changed the choice from “put the real photo in the existing frame” to “compare what the photograph needs to show, while keeping resolution/completion truth separate.”
