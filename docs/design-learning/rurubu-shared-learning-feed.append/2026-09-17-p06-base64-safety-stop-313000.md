# Rurubu shared learning — P06 Base64 safety-stop at 313,000

Date: 2026-09-17 JST
Scope: Rurubu WEDDING V30 P06 `3535:17`
State: `BLOCKED` / failure fingerprint reinforcement

## Observation

Live Figma readback re-verified the canonical owner-master staging stream on node `4249:56` at exactly `313,000 / 4,909,760` Base64 characters with FNV-1a `5b32840c`. The four replaceable photo masks remained present and the visible native fallback remained non-production.

The official lossless `upload_assets` path successfully issued a fresh single-use URL targeting `4249:56`, but the raw PNG POST again failed before HTTP exchange because the execution container could not resolve `mcp.figma.com` (`curl: (6) Could not resolve host`).

After that required one official attempt, the supported guarded `2,001 source -> slice(0, 2,000)` Base64 append was attempted using the exact next canonical characters. The Figma execution safety layer rejected the call before canvas mutation (`safeToRetryWithoutCanvasRead=true`). No chunk was written and the verified checkpoint remained `313,000 / 4,909,760`, FNV `5b32840c`.

## Normalized failure fingerprints

### Official lossless upload
- operation/capability: exact PNG asset upload
- environment/tool path: execution container -> Figma single-use `mcp.figma.com` submit URL
- symptom/error family: DNS resolution failure before HTTP exchange
- likely cause class: container network/DNS boundary
- replacement method: supported Base64 shared-plugin-data bridge
- stop condition: exactly one official upload attempt per run; do not repeat within the same run

### Base64 bridge
- operation/capability: guarded canonical Base64 append
- environment/tool path: `use_figma` shared-plugin-data write on `4249:56`
- symptom/error family: platform safety rejection before Figma execution (`safeToRetryWithoutCanvasRead=true`)
- likely cause class: execution safety layer / payload acceptance, not canonical-source mismatch and not Figma canvas state
- replacement/stop condition: preserve the last verified checkpoint; do not repeat the same guarded payload size in the same unchanged environment. Retry only after a material capability/environment/input-contract change, or use another already-authorized lossless transport primitive.

## Reusable rule

A pre-execution safety rejection is not a corrupt-chunk event. Do not clear or rewrite the previous good key when no mutation occurred. Keep the prior verified length/FNV as authoritative. When the same safety fingerprint has already repeated without a material change, stop shrinking/retrying payloads mechanically; doing so burns runs without increasing evidence or production progress.

## Production impact

- transferred chars this run: `0`
- checkpoint preserved: `313,000 / 4,909,760` (`6.3751%`)
- FNV-1a: `5b32840c`
- visible production: unchanged
- masks: preserved
- `P06 DESIGN_COMPLETE = NO`
