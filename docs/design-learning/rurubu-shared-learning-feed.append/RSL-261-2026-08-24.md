# RSL-261 — Japanese semantic-unit break requires visual + structural correction

Date: 2026-08-24
Source: Rurubu WEDDING / V7 Memory+Guide G6 `2421:2`
State: `VERIFIED_LOCAL → CROSS_ITEM_CANDIDATE`

Visible problem: a compact guide body contained the correct native string `ゆっくり流れる時間。`, but the live composition wrapped it as `...時` / `間。`, creating an amateur Japanese line break at reading scale.

Root-cause hypothesis: text-box technical fit is not equivalent to typographic correctness. Short Japanese semantic units can be damaged by the combined measure/font-size/alignment system even when there is no truncation.

Bounded experiment: clone G5 rollback-safe. A scale-only `22→20 px` attempt still failed visually. A `19 px / 200 px` measure fixed the break but introduced a numeral collision. Final `19 px / x927 / width193` preserved the intact semantic unit and removed the collision.

Three-scale evidence: 500px PASS; 1400px PASS; 1587×1123 PASS. Final visible native text `20`, IMAGE fills `6`, text-text intersections `0`, current-root overlap `0`.

Transferable principle: when Japanese copy has an awkward semantic break, test measure, scale and alignment as a system and require both screenshot and structure readback after each materially different correction. Do not solve it with decorative containment.

Do not transfer: G6's exact 19px size, x-position, width, palette, numbering or Rurubu visual grammar.

Next receiving-item experiment: independently test this QA method on another print artifact when a short Japanese noun/phrase is split unnaturally despite technically valid text geometry.
