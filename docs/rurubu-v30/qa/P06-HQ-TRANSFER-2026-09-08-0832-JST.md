# Rurubu WEDDING V30 — P06 HQ Transfer Evidence — 2026-09-08 08:32 JST

Scope: P06 `3535:17` only. V31 was not created.

## Canonical authority read first

- latest GitHub `main` before this attempt: `2c732524cfe0572fa41dbfb132e06d891a0f0773`
- `docs/rurubu-v30/rurubu-v30-ai-assist-manifest.json`
- `docs/rurubu-v30/P06-HQ-RASTER-PRODUCTION-GUARDRAILS.md`
- `docs/rurubu-v30/P06-HQ-BASE64-BRIDGE-GUARDRAILS.md`
- prior transfer evidence linked by the manifest: `docs/rurubu-v30/qa/P06-HQ-TRANSFER-2026-09-08-0624-JST.md`
- legacy V10 manifest was read only as linked design-learning reference; its stale V10 node IDs did not override V30.

## Canonical Drive source re-verification

Drive file ID: `1mm14Nr1RqlhERZG97Z-9Oz_y-D2lpfWU`

- file: `P06_OWNER_VISUAL_MASTER_HQ_UNCOMPRESSED_20260906.png`
- MIME: `image/png`
- bytes: `3,682,318`
- dimensions: `1055 × 1491`
- mode: `RGBA`
- SHA-256: `13bed2e07d66803d67cd6a117a57bc67dbdd9677b9ba54f4b9dd634910747103`
- canonical Base64 length: `4,909,760`

Authenticated Drive raw retrieval succeeded and materialized the exact canonical PNG in the execution container.

## Live Figma re-verification

Figma file: `bfM0d4c9dCeBv5pCkJ3TNM`

- production P06: `3535:17` remains `V30 P06 / OWNER VM HQ LOSSLESS / TRANSFER PENDING`, `559 × 794`
- exact-byte target: `4249:56`, still pending exact bytes
- four independent replaceable masks remain present: `4000:52`, `4000:55`, `4000:58`, `4000:61`
- fresh design-context read still shows only proxy photo crops + page badge while the owner master is absent
- no V31 and no duplicate P06 frame were created

## Official lossless upload retry

`Figma.upload_assets` issued a fresh single-use upload URL targeting `4249:56` with `scaleMode=FILL`.

The raw multipart POST of the canonical PNG again failed before any HTTP exchange because the execution container cannot resolve `mcp.figma.com` (`curl: (6) Could not resolve host`). No JPEG, preview, lossy derivative, or proxy fallback was used.

## Base64 bridge — live checkpoint correction and append

Fresh live shared-plugin-data read on target `4249:56` found a newer valid checkpoint than the manifest-linked 06:24 evidence:

- non-empty chunks through `b64_0027`
- cumulative Base64 length: `165,000`
- FNV-1a 32-bit: `b03883a5`

The local canonical Base64 prefix at exactly `165,000` characters produced the same FNV checksum, so the pre-append state was verified against the current Drive bytes.

Two new guarded appends were then staged with the validated `8,001 source → slice(0, 8,000) → immediate cumulative verification` primitive:

1. `b64_0028`: stored `8,000`; cumulative `173,000`; FNV `68e634f6` — PASS
2. `b64_0029`: stored `8,000`; cumulative `181,000`; FNV `5297de39` — PASS

Both expected FNV values were computed from the exact local canonical Base64 prefixes before the Figma writes. Each write had a strict precondition on the previous cumulative length/checksum and a rollback path that clears only the newly written key on mismatch. Neither rollback path was needed.

## Current verified checkpoint

- non-empty chunks: `b64_0000` through `b64_0029`
- cumulative verified canonical Base64 prefix: `181,000 / 4,909,760` characters
- cumulative FNV-1a 32-bit: `5297de39`
- visible production artwork remained unchanged during staging

`P06 DESIGN_COMPLETE = NO`.

Continue the official lossless upload retry first on later runs. While container DNS remains blocked, continue only guarded canonical Base64 appends with immediate cumulative verification. Do not reconstruct the Figma image until the complete Base64 stream verifies and decodes to the exact `3,682,318`-byte PNG with the canonical SHA-256. Final P02/P03/P04/P05 high-resolution comparison QA remains intentionally deferred until exact-byte placement succeeds.
