# Rurubu WEDDING V30 — P06 HQ Transfer Evidence

Date: 2026-09-07 21:37 JST
Scope: P06 `3535:17` only. V31 creation is forbidden.

## Canonical-first verification

- Read `docs/rurubu-v30/rurubu-v30-ai-assist-manifest.json` first from current `main`.
- Read `docs/rurubu-v30/P06-HQ-RASTER-PRODUCTION-GUARDRAILS.md` and the latest 19:31 transfer evidence before any production mutation.
- `main` at the start of this run: `bbf92ace8d2e51810128734af6da7deda0e8cb87`.

## Drive source integrity

- Canonical Drive file ID: `1mm14Nr1RqlhERZG97Z-9Oz_y-D2lpfWU`.
- Authenticated raw download succeeded again.
- File: `P06_OWNER_VISUAL_MASTER_HQ_UNCOMPRESSED_20260906.png`.
- MIME: `image/png`; bytes: `3,682,318`.
- Materialized source verified as `1055 × 1491`, RGBA PNG.
- SHA-256: `13bed2e07d66803d67cd6a117a57bc67dbdd9677b9ba54f4b9dd634910747103`.
- Canonical Base64 length: `4,909,760`.
- No proxy, JPEG, preview screenshot, smaller derivative, lossy recompression, duplicate P06, or V31 was used.

## Figma live verification

- Production frame `3535:17` remains `V30 P06 / OWNER VM HQ LOSSLESS / TRANSFER PENDING`, `559 × 794`.
- Exact-byte target `4249:56` remains present and pending.
- Independent replaceable photo masks remain intact: `4000:52`, `4000:55`, `4000:58`, `4000:61`.

## Official lossless upload retry

- `Figma.upload_assets` issued a fresh single-use upload URL targeting `4249:56` with `scaleMode=FILL`.
- Multipart POST of the canonical PNG from the execution container again failed before any HTTP exchange because `mcp.figma.com` could not be resolved (`curl: (6) Could not resolve host`).
- The supported direct byte path therefore remains blocked specifically at container DNS/network → Figma upload endpoint.

## Lossless Base64 staging

Fallback namespace: `rurubu_v30_p06_hq_transfer` on target node `4249:56`.

- Pre-run verified state: `76,000` Base64 characters, cumulative FNV-1a 32-bit `152716729`.
- An oversized new staging write was tested and immediately rejected by integrity checking: the tool transport truncated the payload to `19,999` characters, causing cumulative mismatch.
- The bad `b64_0014` value was removed immediately by clearing only that new key.
- The previous known-good state was then re-verified exactly at `76,000` characters / FNV-1a `152716729`.
- A new safe `12,000`-character canonical segment was staged into `b64_0014`.
- Current staged total: **88,000 Base64 characters**.
- Current cumulative FNV-1a 32-bit: **`972023737`**, matching the canonical local Base64 prefix through offset `87,999`.
- Production visuals were not changed by staging.

## Learning / guardrails update

Created `docs/rurubu-v30/P06-HQ-BASE64-BRIDGE-GUARDRAILS.md` to make the fallback deterministic and safe.

Key addition: new staging writes are capped at **12,000 Base64 characters per `use_figma` call**, with mandatory cumulative length + FNV verification after every chunk. Historical chunk sizes remain variable, so future offsets must be derived from verified cumulative stream length, never from key number.

Bridge guardrails commit: `d2b0b3177301f9bed52f009166b0ed3dfe9edc92`.

## Reconstruction and QA gate

Do not reconstruct until all `4,909,760` Base64 characters pass cumulative verification. After complete staging, concatenate in order, decode to bytes, verify exact byte count `3,682,318`, PNG signature/dimensions `1055 × 1491`, and canonical SHA-256, then create the Figma image and fill `4249:56`.

Only after exact-byte placement succeeds may final P02/P03/P04/P05 comparison and fresh high-resolution P06 QA clear transfer-pending status.

## Status

`P06 DESIGN_COMPLETE = NO`

No user decision is required. Continue the official lossless upload retry first on later runs and continue integrity-gated Base64 staging as the fallback bridge. Do not weaken the production-source rule.
