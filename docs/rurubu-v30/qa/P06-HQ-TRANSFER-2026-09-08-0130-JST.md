# Rurubu WEDDING V30 — P06 HQ Transfer Evidence

Date: 2026-09-08 01:30 JST
Scope: P06 `3535:17` only. V31 creation is forbidden.

## Canonical-first verification

- Read `docs/rurubu-v30/rurubu-v30-ai-assist-manifest.json` first from current `main`.
- Read `docs/rurubu-v30/P06-HQ-RASTER-PRODUCTION-GUARDRAILS.md`, `docs/rurubu-v30/P06-HQ-BASE64-BRIDGE-GUARDRAILS.md`, and previous transfer evidence before writes.

## Drive source integrity

- Re-verified Drive file ID `1mm14Nr1RqlhERZG97Z-9Oz_y-D2lpfWU`, exact filename `P06_OWNER_VISUAL_MASTER_HQ_UNCOMPRESSED_20260906.png`, MIME `image/png`, bytes `3,682,318`.
- Authenticated raw Drive download materialized successfully.
- Local verification: `1055 × 1491`, RGBA PNG, SHA-256 `13bed2e07d66803d67cd6a117a57bc67dbdd9677b9ba54f4b9dd634910747103`, Base64 length `4,909,760`.

## Official lossless upload retry

- `Figma.upload_assets` successfully issued a fresh single-use upload URL targeting `4249:56` with `scaleMode=FILL`.
- Multipart POST of the canonical PNG failed before HTTP exchange because the execution container still cannot resolve `mcp.figma.com` (`curl: (6) Could not resolve host`).
- No proxy, JPEG, screenshot, resize, lossy recompression, duplicate frame, or V31 was used.

## Base64 fallback staging

Namespace: `rurubu_v30_p06_hq_transfer` on exact target node `4249:56`.

- Pre-write live Figma readback verified the previous known-good state: `113,000` Base64 characters, FNV-1a `6e4c1d25`, 20 non-empty chunks through `b64_0019`.
- Appended canonical `b64_0020` with exactly `12,000` characters from canonical Base64 offset `113,000`.
- Immediate cumulative verification passed: **`125,000` Base64 characters**, FNV-1a **`d19aeb56`**, 21 non-empty chunks, matching the locally computed canonical prefix.
- Visible production artwork and four replaceable photo masks were not changed.

## QA / status

- Exact-byte reconstruction remains gated until all `4,909,760` Base64 characters verify.
- Final P02/P03/P04/P05 comparison QA remains deferred until exact HQ bytes are applied to `4249:56`.
- P06 remains transfer-pending and `DESIGN_COMPLETE = NO`.
- No user decision is required; continue official lossless upload first on subsequent runs, then integrity-gated Base64 staging while the DNS blocker persists.
