# Rurubu WEDDING V30 — P06 HQ Transfer Evidence — 2026-09-08 03:30 JST

Scope: P06 `3535:17` only. V31 was not created.

## Canonical authority read first

- `docs/rurubu-v30/rurubu-v30-ai-assist-manifest.json`
- `docs/rurubu-v30/P06-HQ-RASTER-PRODUCTION-GUARDRAILS.md`
- `docs/rurubu-v30/P06-HQ-BASE64-BRIDGE-GUARDRAILS.md`

## Canonical source re-verification

- Drive file ID: `1mm14Nr1RqlhERZG97Z-9Oz_y-D2lpfWU`
- File: `P06_OWNER_VISUAL_MASTER_HQ_UNCOMPRESSED_20260906.png`
- MIME: `image/png`
- Bytes: `3,682,318`
- Dimensions: `1055 × 1491`
- SHA-256: `13bed2e07d66803d67cd6a117a57bc67dbdd9677b9ba54f4b9dd634910747103`
- Canonical Base64 length: `4,909,760`

Drive authenticated raw retrieval succeeded and the exact PNG materialized into the execution container.

## Live Figma re-verification

Figma file: `bfM0d4c9dCeBv5pCkJ3TNM`

- Production P06: `3535:17` remains `V30 P06 / OWNER VM HQ LOSSLESS / TRANSFER PENDING`.
- Exact-byte target: `4249:56` remains present at `559 × 794`.
- Replaceable masks remain intact: `4000:52`, `4000:55`, `4000:58`, `4000:61`.
- No V31 or duplicate P06 frame was created.

## Official lossless upload retry

`Figma.upload_assets` successfully issued a fresh single-use upload URL for `4249:56` with `scaleMode=FILL`.

Raw multipart POST of the canonical PNG from the container failed before any HTTP exchange:

`curl: (6) Could not resolve host: mcp.figma.com`

The unresolved official-upload segment therefore remains container DNS/network → Figma upload endpoint.

## Base64 fallback integrity investigation

The previously staged lossless prefix was read from shared plugin data namespace `rurubu_v30_p06_hq_transfer`:

- non-empty keys: `25`
- cumulative length: `145,000`
- FNV-1a 32-bit: `7a102c42`

This exactly matches the canonical Base64 prefix in the container, so the historical staged prefix is still known-good.

Two new staging attempts were performed under the guardrail limit and immediately verified:

1. A nominal 12,000-character append arrived as `11,999` characters and cumulative checksum mismatched.
2. After clearing that bad key and restoring the known-good prefix, a nominal 4,000-character append arrived as `3,999` characters and cumulative checksum mismatched.

In both cases only the newly written bad key `b64_0025` was cleared, and the previous known-good state was re-verified afterward:

- cumulative length restored to `145,000`
- FNV-1a 32-bit restored to `7a102c42`
- non-empty keys restored to `25`

No visible production artwork changed during staging tests. No JPEG, preview, resized derivative, lossy recompression, duplicate frame, or simulated completion was used.

## Current status

`P06 DESIGN_COMPLETE = NO`.

The exact source is healthy, Drive retrieval is healthy, Figma target/authorization is healthy, and the historical Base64 prefix is healthy. Two byte-transport paths are currently blocked:

- official raw upload: container DNS cannot resolve `mcp.figma.com`;
- shared-plugin-data fallback: newly supplied literal chunks are not arriving byte-for-byte intact in this remote execution surface, so checksum guardrails correctly reject them.

Do not weaken the production-source rule. Continue supported lossless transfer investigation on later runs. Final P02/P03/P04/P05 cross-page visual QA remains deferred until exact-byte placement succeeds.
