# Rurubu WEDDING V30 — P06 HQ Transfer Evidence — 2026-09-08 06:24 JST

Scope: P06 `3535:17` only. V31 was not created.

## Canonical authority read first

- latest GitHub `main`: `61771177dea87810048bc3cbb4537dce8df15a12`
- `docs/rurubu-v30/rurubu-v30-ai-assist-manifest.json`
- `docs/rurubu-v30/P06-HQ-RASTER-PRODUCTION-GUARDRAILS.md`
- `docs/rurubu-v30/P06-HQ-BASE64-BRIDGE-GUARDRAILS.md`
- `docs/rurubu-v30/qa/P06-HQ-TRANSFER-2026-09-08-0330-JST.md`
- legacy V10 manifest linked by the V30 manifest was read only as design-learning reference; its stale V10 production node IDs did not override V30 live state.

## Canonical Drive source re-verification

Drive file ID: `1mm14Nr1RqlhERZG97Z-9Oz_y-D2lpfWU`

- file: `P06_OWNER_VISUAL_MASTER_HQ_UNCOMPRESSED_20260906.png`
- MIME: `image/png`
- bytes: `3,682,318`
- dimensions: `1055 × 1491`
- mode: `RGBA`
- SHA-256: `13bed2e07d66803d67cd6a117a57bc67dbdd9677b9ba54f4b9dd634910747103`
- canonical Base64 length: `4,909,760`

Authenticated Drive raw retrieval succeeded and materialized the exact canonical PNG in the execution container.

## Live Figma re-verification

Figma file: `bfM0d4c9dCeBv5pCkJ3TNM`

- production page: `V30_FINAL_PRODUCTION` (`3545:2`)
- production P06: `3535:17` remains `V30 P06 / OWNER VM HQ LOSSLESS / TRANSFER PENDING`, `559 × 794`
- exact-byte target: `4249:56`, `559 × 794`, still has no visible image fill
- independent replaceable masks remain intact:
  - `4000:52` main, `290 × 234`
  - `4000:55` food, `175 × 147`
  - `4000:58` pets, `162 × 147`
  - `4000:61` travel, `160 × 148`
- no V31 and no duplicate P06 frame were created

## Official lossless upload retry

`Figma.upload_assets` issued a fresh single-use upload URL targeting `4249:56` with `scaleMode=FILL`.

The canonical PNG raw POST still failed before any HTTP exchange because the execution container cannot resolve/reach `mcp.figma.com`. The Weave upload surface was also inspected and uses the same `mcp.figma.com` host, so it does not bypass this network boundary.

No proxy/JPEG/lossy fallback was used.

## Base64 fallback — live state correction

Shared plugin data is stored on target node `4249:56` under namespace `rurubu_v30_p06_hq_transfer`, not on the document root.

Fresh live read found a newer valid state than the 03:30 evidence:

- keys present through `b64_0025`
- non-empty chunks: `26`
- cumulative canonical Base64 prefix: `149,000` characters
- FNV-1a 32-bit: `0237d2e8`

The same `149,000`-character prefix from the canonical local PNG produced the identical FNV checksum, so this state was verified byte-for-byte at the Base64 level.

## New validated transport technique

A nominal large literal remains unreliable: a 12,000-character append attempt arrived truncated and failed cumulative verification. The code immediately cleared only the new bad key, preserving the known-good prefix.

A new guarded technique was then tested successfully:

1. take exactly the next `8,000` canonical Base64 characters;
2. include one additional canonical character as a transport guard (`8,001` source characters total);
3. inside the Figma execution context, explicitly use `source.slice(0, 8000)` before `setSharedPluginData`;
4. immediately concatenate all staged chunks and verify expected cumulative length + FNV-1a against the canonical local prefix;
5. clear only the new key on any mismatch.

Result for `b64_0026`:

- source literal observed by Figma: `8,001` characters
- stored chunk: exactly `8,000` characters
- cumulative verified prefix: `157,000` characters
- cumulative FNV-1a: `51df2c42`
- local canonical prefix at `157,000` characters: identical length and checksum

Therefore `b64_0000` through `b64_0026` now form a verified lossless canonical prefix of `157,000 / 4,909,760` Base64 characters.

This is meaningful progress because the fallback is no longer limited to historically staged data: a new chunk was transported and verified successfully in the current remote Figma execution surface without changing visible artwork.

## Current status

`P06 DESIGN_COMPLETE = NO`.

Exact-byte transfer is still in progress. The official upload path remains blocked by container network/DNS, but the guarded Base64 staging path has a newly validated working append primitive. Continue staging only canonical data with immediate cumulative verification. Do not reconstruct the image or run final P02/P03/P04/P05 visual QA until the full `4,909,760` Base64 characters verify and reconstruct to the exact `3,682,318`-byte PNG with the canonical SHA-256.
