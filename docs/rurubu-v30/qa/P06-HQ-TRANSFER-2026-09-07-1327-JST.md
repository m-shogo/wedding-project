# Rurubu WEDDING V30 — P06 HQ Transfer Evidence

Date: 2026-09-07 13:27 JST
Scope: P06 `3535:17` only. V31 creation is forbidden.

## Canonical-first verification

- Read `docs/rurubu-v30/rurubu-v30-ai-assist-manifest.json` first from current `main`.
- Read `docs/rurubu-v30/P06-HQ-RASTER-PRODUCTION-GUARDRAILS.md` and the previous 12:27 transfer evidence before production work.
- Production remains restricted to Figma file `bfM0d4c9dCeBv5pCkJ3TNM`, page `V30_FINAL_PRODUCTION`, frame `3535:17`.

## Drive source integrity

- Canonical file: `P06_OWNER_VISUAL_MASTER_HQ_UNCOMPRESSED_20260906.png`.
- Drive file ID: `1mm14Nr1RqlhERZG97Z-9Oz_y-D2lpfWU`.
- Authenticated raw download succeeded again as `image/png`, `3,682,318` bytes.
- Materialized source remains a real `1055 × 1491` RGBA PNG.
- SHA-256 remains `13bed2e07d66803d67cd6a117a57bc67dbdd9677b9ba54f4b9dd634910747103`.
- No JPEG proxy, preview screenshot, lossy derivative, smaller production derivative, duplicate P06 frame, or V31 was used.

## Fresh live Figma verification

- `V30_FINAL_PRODUCTION` is page `3545:2`.
- P06 `3535:17` remains `V30 P06 / OWNER VM HQ LOSSLESS / TRANSFER PENDING` at `559 × 794`.
- Exact-byte target remains `4249:56`, still named `P06_OWNER_VM_HQ_UNCOMPRESSED / 1055x1491 / SCALE_0.53<=0.70 / PENDING_EXACT_BYTES`.
- Independent replaceable photo masks remain intact: `4000:52`, `4000:55`, `4000:58`, `4000:61`.

## New lossless transfer progress

The prior text-staging path had two separate issues that are now distinguished:

1. A shared-plugin-data namespace containing hyphens is invalid in the current Figma Plugin API surface. The exact runtime error is: `The namespace can only consist of alphanumeric characters, _ or .`.
2. The previous large Base32768 payload also proved unsafe because its model-mediated text was altered before storage.

The namespace is now corrected to `rurubu_v30_p06_hq_transfer` and the payload format is changed to ordinary ASCII Base64 with small integrity-checked chunks.

A real first chunk was persisted to target node `4249:56`:

- key: `b64_0000`
- source Base64 offset: `0`
- chunk length: `4,000` characters
- expected FNV-1a 32-bit: `737522881`
- literal pre-storage validation: PASS (`length=4000`, `FNV=737522881`)
- live Figma readback validation: PASS (`length=4000`, `FNV=737522881`)
- exact string equality before/after storage: PASS
- total Base64 stream length for the canonical PNG: `4,909,760` characters
- verified staged progress: `4,000 / 4,909,760` characters (`~0.0815%`)

This is the first proven byte-preserving bridge that does not depend on container DNS access to `mcp.figma.com`. It is intentionally slow and must remain integrity-gated.

## Reconstruction gate

Image reconstruction remains forbidden until **every** Base64 chunk has passed per-chunk length + checksum + equality validation. After full staging, plugin-main must:

1. concatenate chunks in declared order;
2. decode Base64 to bytes;
3. verify final byte count is exactly `3,682,318`;
4. verify PNG signature and dimensions `1055 × 1491`;
5. verify final source checksum against SHA-256 `13bed2e07d66803d67cd6a117a57bc67dbdd9677b9ba54f4b9dd634910747103` using an available trustworthy validation route;
6. only then call `figma.createImage(Uint8Array)` and assign the resulting image hash to `4249:56`;
7. perform fresh P02/P03/P04/P05 comparison and high-resolution P06 QA before clearing transfer-pending status.

## Status

`P06 DESIGN_COMPLETE = NO`

No user decision is required. The production frame and all four replaceable photo masks remain intact. Continue the verified small-chunk Base64 staging path on subsequent work batches; do not weaken the production-source rule.