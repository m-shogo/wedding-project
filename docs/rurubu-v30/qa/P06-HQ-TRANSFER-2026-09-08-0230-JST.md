# Rurubu WEDDING V30 — P06 HQ Transfer Evidence

Date: 2026-09-08 02:30 JST
Scope: P06 `3535:17` only. V31 creation is forbidden.

## Canonical-first verification

- Read `docs/rurubu-v30/rurubu-v30-ai-assist-manifest.json` first from current `main`.
- Read `docs/rurubu-v30/P06-HQ-RASTER-PRODUCTION-GUARDRAILS.md`, `docs/rurubu-v30/P06-HQ-BASE64-BRIDGE-GUARDRAILS.md`, and the previous transfer evidence before writes.

## Drive source integrity

- Re-verified Drive file ID `1mm14Nr1RqlhERZG97Z-9Oz_y-D2lpfWU`, exact filename `P06_OWNER_VISUAL_MASTER_HQ_UNCOMPRESSED_20260906.png`, MIME `image/png`, bytes `3,682,318`.
- Authenticated raw Drive download materialized successfully.
- Local verification remains `1055 × 1491`, RGBA PNG, SHA-256 `13bed2e07d66803d67cd6a117a57bc67dbdd9677b9ba54f4b9dd634910747103`, Base64 length `4,909,760`.

## Live Figma preflight

- `3535:17` remains `V30 P06 / OWNER VM HQ LOSSLESS / TRANSFER PENDING`.
- Exact-byte target `4249:56` remains fill-less before reconstruction.
- Replaceable masks `4000:52`, `4000:55`, `4000:58`, `4000:61` all remain present and independent.
- Pre-run verified Base64 staging state: `125,000` characters, FNV-1a `d19aeb56`, 21 non-empty chunks through `b64_0020`.

## Official lossless upload retry

- `Figma.upload_assets` issued a fresh single-use upload URL targeting `4249:56` with `scaleMode=FILL`.
- Raw-byte POST remains blocked at the same container DNS/network boundary to `mcp.figma.com`; no lower-quality fallback was used.

## Base64 fallback staging

Namespace: `rurubu_v30_p06_hq_transfer` on exact target node `4249:56`.

- A 12,000-character `b64_0021` write did not match the expected canonical prefix checksum (`2187c6e5` observed vs `aa8f2101` expected) and was immediately rolled back to empty per guardrail.
- Retried with smaller integrity-gated chunks sourced directly from the canonical Base64 stream:
  - `b64_0021`: `4,000` chars → cumulative `129,000`, FNV-1a `f6880fee` — PASS.
  - `b64_0022`: `4,000` chars → cumulative `133,000`, FNV-1a `75ac73ec` — PASS.
  - `b64_0023`: `4,000` chars → cumulative `137,000`, FNV-1a `aa8f2101` — PASS.
  - `b64_0024`: `8,000` chars → cumulative **`145,000`**, FNV-1a **`7a102c42`** — PASS.
- Net verified progress this run: `+20,000` canonical Base64 characters.
- Visible production artwork was not changed during staging.

## QA / status

- Exact-byte reconstruction remains gated until all `4,909,760` Base64 characters verify.
- Final P02/P03/P04/P05 comparison QA remains deferred until the exact HQ PNG is applied to `4249:56`.
- P06 remains `DESIGN_COMPLETE = NO`.
- No user decision is required. Continue attempting the official lossless route first on later runs, then continue integrity-gated Base64 staging while the DNS blocker persists.
