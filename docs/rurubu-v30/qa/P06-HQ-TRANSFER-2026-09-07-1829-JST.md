# Rurubu WEDDING V30 — P06 HQ Transfer Evidence

Date: 2026-09-07 18:29 JST
Scope: P06 `3535:17` only. V31 creation is forbidden.

## Canonical-first verification

- Read `docs/rurubu-v30/rurubu-v30-ai-assist-manifest.json` first from current `main`.
- Read `docs/rurubu-v30/P06-HQ-RASTER-PRODUCTION-GUARDRAILS.md` and the latest 17:29 transfer evidence before any production mutation.
- `main` at the start of this run: `7e74ab6825ca1e964e1fc1f4f23f8b950dd9af00`.

## Drive source integrity

- Canonical Drive file ID: `1mm14Nr1RqlhERZG97Z-9Oz_y-D2lpfWU`.
- Authenticated raw download succeeded again.
- File: `P06_OWNER_VISUAL_MASTER_HQ_UNCOMPRESSED_20260906.png`.
- MIME: `image/png`; bytes: `3,682,318`.
- Materialized source verified as `1055 × 1491`, RGBA PNG.
- SHA-256 remains `13bed2e07d66803d67cd6a117a57bc67dbdd9677b9ba54f4b9dd634910747103`.
- No proxy, JPEG, preview screenshot, smaller derivative, lossy recompression, duplicate P06, or V31 was used.

## Figma live verification

- Production frame `3535:17` remains `V30 P06 / OWNER VM HQ LOSSLESS / TRANSFER PENDING`, `559 × 794`.
- Exact-byte target `4249:56` remains present and pending.
- Independent replaceable photo masks remain intact: `4000:52`, `4000:55`, `4000:58`, `4000:61`.

## Official lossless upload retry

- `Figma.upload_assets` issued a fresh single-use upload URL targeting `4249:56` with `scaleMode=FILL`.
- Multipart POST of the canonical PNG from the execution container again failed before any HTTP exchange because `mcp.figma.com` could not be resolved (`curl: (6) Could not resolve host`).
- The direct supported byte path therefore remains blocked specifically at container DNS/network → Figma upload endpoint.

## Lossless Base64 staging progress

The fallback bridge continues in shared plugin data namespace `rurubu_v30_p06_hq_transfer` on target node `4249:56`.

The prior verified contiguous staged range was offsets `0..51,999` = 52,000 Base64 characters.

This run successfully persisted and read back three more checksum-gated 4,000-character chunks:

| key | Base64 offset | length | FNV-1a 32-bit | result |
|---|---:|---:|---:|---|
| `b64_0008` | 52,000 | 4,000 | `154328168` | PASS |
| `b64_0009` | 56,000 | 4,000 | `2959547078` | PASS |
| `b64_0010` | 60,000 | 4,000 | `3411425350` | PASS |

- A prior batched attempt containing `b64_0011` was rejected atomically by the in-script length gate because the transmitted literal arrived truncated; no partial/bad write occurred.
- `b64_0008` through `b64_0010` were then written individually and each passed exact live readback equality plus FNV-1a verification.
- Verified contiguous staged range is now offsets `0..63,999` = **64,000 characters** (`~1.3035%` of the total `4,909,760` Base64 characters).
- Production visuals were not changed by staging.

## Reconstruction and QA gate

Do not reconstruct until the full Base64 stream has passed per-chunk verification. After complete staging, concatenate in order, decode to bytes, verify exact byte count `3,682,318`, PNG signature/dimensions `1055 × 1491`, and canonical checksum, then create the Figma image and fill `4249:56`. Only after exact-byte placement succeeds may final P02/P03/P04/P05 comparison and fresh high-resolution P06 QA clear transfer-pending status.

## Status

`P06 DESIGN_COMPLETE = NO`

No user decision is required. Continue the official lossless upload retry first on later runs and continue only integrity-gated small ASCII Base64 staging as the fallback bridge. Do not weaken the production-source rule.