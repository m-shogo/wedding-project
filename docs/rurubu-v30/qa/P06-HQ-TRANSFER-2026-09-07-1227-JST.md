# Rurubu WEDDING V30 — P06 HQ Transfer Evidence

Date: 2026-09-07 12:27 JST
Scope: P06 `3535:17` only. V31 creation is forbidden.

## Canonical-first verification

- Latest GitHub `main` observed before this attempt: `261bbac6fb9294639801a4c01516fd4a5d588e7f`.
- Read `docs/rurubu-v30/rurubu-v30-ai-assist-manifest.json` first.
- Read `docs/rurubu-v30/P06-HQ-RASTER-PRODUCTION-GUARDRAILS.md`, latest actual 11:31 transfer evidence, and linked legacy design-learning authority before production work.

## Drive source integrity

- Canonical file: `P06_OWNER_VISUAL_MASTER_HQ_UNCOMPRESSED_20260906.png`.
- Drive file ID: `1mm14Nr1RqlhERZG97Z-9Oz_y-D2lpfWU`.
- Authenticated raw download succeeded again as `image/png`, `3,682,318` bytes.
- Exact owner master materialized locally at `/mnt/data/P06_OWNER_VISUAL_MASTER_HQ_UNCOMPRESSED_20260906.png`.
- SHA-256 remains `13bed2e07d66803d67cd6a117a57bc67dbdd9677b9ba54f4b9dd634910747103`.
- No JPEG proxy, preview, lower-resolution reference, lossy derivative, duplicate frame, or V31 was used.

## Fresh live Figma readback

- Production frame `3535:17` remains `V30 P06 / OWNER VM HQ LOSSLESS / TRANSFER PENDING` at `559 × 794`.
- HQ target `4249:56` remains present and pending exact bytes.
- Independent replaceable masks remain intact: `4000:52`, `4000:55`, `4000:58`, `4000:61`.
- Fresh design-context rendering still shows the four proxy photo crops plus PAGE badge over the blank paper field; exact owner-master art is not present yet.

## Lossless transfer attempt

- Official Figma `upload_assets` successfully issued another single-use upload target for `4249:56` with `scaleMode=FILL`, so Figma authorization and target selection remain healthy.
- Container raw-byte delivery remains unavailable at the DNS/network boundary; the official endpoint cannot be reached from the container.
- The persistent `setSharedPluginData` bridge was advanced from a probe to a real encoded-byte staging test. A Base32768 representation of the exact PNG was prepared locally so Figma can eventually reconstruct a `Uint8Array` entirely in plugin-main and call `figma.createImage` without lossy conversion.
- A small 50-character shared-plugin-data round-trip was exact, proving Unicode staging itself works.
- The first large model-mediated staging payload was **rejected**: the intended encoded chunk length was `16,000` characters, while live Figma readback returned `17,052`. This means the transfer text was altered/duplicated before storage and is not byte-exact.
- The invalid staging node and probe node were immediately removed. No reconstruction, image creation, fill assignment, or visible production mutation was attempted from invalid data.

## Recovery rule added by this attempt

Large unverified model-mediated transfer strings must never be trusted. Any future shared-plugin-data route must use smaller chunks with per-chunk exact length **and checksum validation** before the chunk is accepted. Reconstruction is forbidden unless every chunk passes integrity validation and the final byte count, PNG signature, dimensions `1055 × 1491`, and source checksum are verified.

## Status

`P06 DESIGN_COMPLETE = NO`

No user decision is required. The production frame and all four replaceable photo masks remain clean and unchanged. Continue lossless transfer work only with integrity-checked staging; final P02/P03/P04/P05 comparison and high-resolution production QA remain gated until the exact owner-master bytes are present in live Figma.
