# Rurubu WEDDING V30 — P06 HQ Transfer Evidence

Date: 2026-09-07 23:29 JST
Scope: P06 `3535:17` only. V31 creation is forbidden.

## Canonical-first verification

- Read `docs/rurubu-v30/rurubu-v30-ai-assist-manifest.json` first from current `main`.
- Read `docs/rurubu-v30/P06-HQ-RASTER-PRODUCTION-GUARDRAILS.md` and `docs/rurubu-v30/P06-HQ-BASE64-BRIDGE-GUARDRAILS.md` before transfer work.
- `main` at verification: `15c6001e9fa18c787f7adcc3be20a62100c58866`.

## Drive source integrity

- Canonical Drive file ID: `1mm14Nr1RqlhERZG97Z-9Oz_y-D2lpfWU`.
- Authenticated raw download succeeded.
- File: `P06_OWNER_VISUAL_MASTER_HQ_UNCOMPRESSED_20260906.png`.
- MIME: `image/png`; bytes: `3,682,318`.
- Local SHA-256 re-verified: `13bed2e07d66803d67cd6a117a57bc67dbdd9677b9ba54f4b9dd634910747103`.
- Canonical Base64 length remains `4,909,760`.

## Official lossless upload retry

- `Figma.upload_assets` issued a fresh single-use upload URL targeting `4249:56` with `scaleMode=FILL`.
- Multipart POST of the exact canonical PNG again failed before HTTP exchange because the execution container cannot resolve `mcp.figma.com` (`curl: (6) Could not resolve host`).
- No proxy, JPEG, screenshot, resized derivative, lossy recompression, duplicate P06, or V31 was used.

## Base64 fallback staging

Namespace: `rurubu_v30_p06_hq_transfer` on exact target node `4249:56`.

- Pre-run known-good staged state: `89,000` Base64 characters; FNV-1a 32-bit hex `b0257f51`, matching the canonical local prefix.
- One attempted `b64_0016` write exceeded the safe payload after tool-side text handling (`14,862` chars). Mandatory cumulative FNV verification detected mismatch immediately.
- Only the newly written bad key was cleared, then the prior `89,000` / `b0257f51` state was re-verified exactly before continuing.
- Safe 4,000-character canonical chunks were then appended as `b64_0016`, `b64_0017`, and `b64_0018`, each followed by cumulative verification.
- Current verified staged total: **101,000 Base64 characters**.
- Current FNV-1a 32-bit hex: **`f04a3967`**, exactly matching the local canonical Base64 prefix through offset `100,999`.
- Visible production artwork was not changed; exact-byte reconstruction remains gated until all `4,909,760` characters verify.

## QA / status

- Final P02/P03/P04/P05 comparison QA remains intentionally deferred until exact HQ bytes are reconstructed and applied to `4249:56`.
- P06 remains transfer-pending and `DESIGN_COMPLETE = NO`.
- No user decision is required. Continue official lossless upload first on later runs, then continue integrity-gated Base64 staging while DNS remains blocked.
