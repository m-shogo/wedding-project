# Rurubu WEDDING V30 — P06 HQ Transfer Evidence

Date: 2026-09-07 14:30 JST
Scope: P06 `3535:17` only. V31 creation is forbidden.

## Canonical-first verification

- Read `docs/rurubu-v30/rurubu-v30-ai-assist-manifest.json` first from current `main`.
- Read `docs/rurubu-v30/P06-HQ-RASTER-PRODUCTION-GUARDRAILS.md` and the 13:27 transfer evidence before any production mutation.
- `main` at the start of this run: `d52bdee9497b8c6361cc52b166014526a663969f`.

## Drive source integrity

- Canonical file ID: `1mm14Nr1RqlhERZG97Z-9Oz_y-D2lpfWU`.
- Authenticated raw download succeeded again.
- MIME: `image/png`; bytes: `3,682,318`.
- Materialized source SHA-256 remains `13bed2e07d66803d67cd6a117a57bc67dbdd9677b9ba54f4b9dd634910747103`.
- No proxy, JPEG, preview screenshot, smaller derivative, lossy recompression, duplicate P06, or V31 was used.

## Figma live/staging verification

- Target node `4249:56` still exists and remains transfer-pending.
- Shared-plugin-data namespace `rurubu_v30_p06_hq_transfer` still contains only the three previously verified chunks: `b64_0000` = 4,000 chars, `b64_0001` = 12,000 chars, `b64_0002` = 12,000 chars; verified contiguous staged total remains 28,000 chars.
- A 40,000-character candidate chunk was rejected by literal precheck because the model-mediated tool payload arrived truncated; nothing was stored.
- A 12,000-character `b64_0003` candidate was also rejected by literal precheck because the payload arrived with a one-character mutation (`12,001` chars instead of `12,000`); nothing was stored.
- These failures confirm the integrity gate is functioning correctly: malformed text never reaches persistent staging.

## Official lossless upload retry

- `Figma.upload_assets` successfully issued a fresh single-use upload URL targeting `4249:56` with `scaleMode=FILL`.
- Multipart POST of the canonical PNG from the execution container again failed before any HTTP exchange with `curl: (6) Could not resolve host: mcp.figma.com`.
- Therefore the direct supported byte path remains blocked at container DNS/network → Figma upload endpoint.

## Status

`P06 DESIGN_COMPLETE = NO`

No user decision is required. Production visuals and the four replaceable photo masks remain untouched. Continue retrying the official lossless upload path and only retain text-staged chunks that pass literal length/checksum plus live Figma readback equality. Final P02/P03/P04/P05 comparison QA remains gated on exact-byte image placement.