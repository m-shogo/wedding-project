# るるぶWEDDING V6 — P + AX/AS Visual QA

Date: 2026-08-16
State: `VERIFIED_LOCAL_DUMMY_DESIGN_STUDY / ROLLBACK_SAFE / V7_HOLD / NOT_PRINT_READY`

## Current preferred

- Outer P `1397:64`
- Profile/Q&A AX `1406:2`
- Story/chronology AS `1392:2`
- Start Here `845:27` = `V5 FU/FX · V6 P + AX/AS INSIDE STUDIES · V7 HOLD`

AX supersedes AV only on the Profile photo-cluster geometry. Q&A geometry/content in AX is unchanged from the verified AV state.

## Why AX

AV removed AU's sparse lower-page feeling and passed whole/read/actual-size QA, but actual Profile review still showed a passive final strip beneath the three-photo cluster.

AX tests one bounded change only: use more of the existing physical page field with the already verified replaceable photos instead of filling the remaining space with additional decorative raster or native card geometry.

Final Profile lower collage:

- snapshot 1 `410×280`, x `14`, y `720`;
- snapshot 2 `340×245`, x `275`, y `805`;
- snapshot 3 `238×185`, x `520`, y `720`;
- all three remain replaceable IMAGE roles;
- no new asset, text, card, shadow, gradient or generated decoration.

The source roles remain within their previously verified source-quality limits. The smallest skyline role remains capped below its verified `240×220` source role.

## Visual evidence

- 500 px whole spread: PASS; the Profile page has a stronger lower visual anchor and reads less like a template with unused paper below;
- reading-scale comparison: PASS;
- actual Profile `794×1123`: PASS; photos create one deliberate overlapping memory cluster instead of three separated thumbnails;
- Q&A actual-size result is unchanged from AV and remains PASS.

## Structure

AX Profile `1406:3`:

- visible native text `17`;
- replaceable IMAGE roles `4`;
- text/text collision `0`;
- accidental text/image collision `0`;
- 18 px text safe-area risk `0`;
- intentional Profile hero pullquote overlap remains unchanged.

AX Q&A is an exact geometry continuation of AV Q&A:

- native text `24`;
- replaceable IMAGE roles `2`;
- prior AV structural PASS applies because AX did not modify Q&A descendants.

## Dynamic copy evidence

AV-specific long-answer proof `1405:2` remains applicable to AX Q&A because AX changed Profile image geometry only and left the Q&A subtree unchanged.

That proof already established:

- six realistic longer Japanese answers;
- text/text collision `0`;
- accidental text/image collision `0`;
- 18 px safe-area risk `0`;
- outside-page text `0`.

## Comparison history

- AU `1394:2` — hidden rollback after AV promotion;
- AV `1403:2` — hidden rollback after AX promotion;
- AW `1403:64` — hidden rejected composed-decoration study;
- AX `1406:2` — preferred.

AW remains useful negative evidence: already-resident low-resolution composed accents did not outperform the stronger photo/type baseline and were not adopted.

## Asset lifecycle truth

- new generation: `0`
- new Drive write: `0`
- new external upload: `0`
- existing verified photos recomposed: `YES`
- fixed/generated decoration adopted: `NO`
- native variable text preserved: `YES`
- image replacement semantics preserved: `YES`
- V7 touched: `NO`

Drive V6 root remains `1wHxC2E09JpLIQRNDDTY4i29KMwMY2_XK` and generated section masters remain present but not newly placed/adopted.

## Decision

`P + AX/AS` is the current verified-local V6 dummy-design set.

This is not a final/print-ready declaration. Final real photography/copy, exact printer template, trim/bleed/fold/page order, PDF preflight and physical proof remain open.