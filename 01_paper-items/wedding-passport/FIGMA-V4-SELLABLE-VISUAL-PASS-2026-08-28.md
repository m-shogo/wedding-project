# WEDDING PASSPORT V4 — Sellable Visual Pass

Date: 2026-08-28
Latest main before write: `8bd184df7bd2a6bea84a304fe10c42b4b0f72933`
Current authority: `docs/automation/non-rurubu-figma-quality-current.md`
Figma: `UbK8KmuWJcDeGScsN49Uor`
State: `SELLABLE_VISUAL_QA_PASS + DESIGN_QA_PASS_WITH_PLACEHOLDERS / V4_CURRENT / NOT_PRINT_READY`

## Why this visual gate can close

The V4 family was rebuilt from blank frames and now clears the reopened sellable-art-direction gate without reusing the old visual system.

Final four-face read:

- Front `226:2`: centered deep-navy passport-book hierarchy, original editable celestial identity, muted-gold title/date, native variable couple names. No web-card/admin grammar.
- Back `226:15`: quiet asymmetric closing page, strong Japanese headline, warm tactile paper substrate, deep-navy physical field, native date/place/message/couple roles.
- Menu+Drink `226:26`: clear unequal information hierarchy, no rounded-card UI, print-oriented two-zone composition, newly adopted archival paper field.
- Seating `226:71`: 11 tables × maximum seven guests, lookup-first two-column structure, names prioritized over decoration, archival paper field, no decorative center panel.

Fresh reference comparison used the exact non-Rurubu Passport direction image `1Dd4cVUaE0lcMPya8Hez40OHGNTReZ6J5`. The reference remains the quality floor rather than a literal composition template. The V4 now has comparable physical-book identity and paper tactility while materially improving variable-text editability and seating/menu readability.

## Three-scale QA

Fresh same-scale thumbnails were rendered for all four roots after the paper-field adoption. Front / Back / Menu+Drink / Seating each retain a clear first read and no equal-card/dashboard impression.

Reading-scale QA was re-run on the interiors after texture adoption. Menu+Drink and Seating remain legible; the paper field stays subordinate to typography.

Actual-size geometry render was re-run for Menu+Drink at native `1480 × 2100`. Prior front native render and seating print-readability/long-name stress evidence remain valid because the new fixed paper field did not alter text geometry.

## Structure QA

Post-adoption readback:

- Back: visible text `7`, out-of-bounds `0`, IMAGE-fill nodes `0`;
- Menu+Drink: visible text `36`, out-of-bounds `0`, IMAGE-fill nodes `0`;
- Seating: visible text `37`, out-of-bounds `0`, IMAGE-fill nodes `0`.

Variable/factual copy remains native Figma text. The fixed cover identity and paper substrates are editable vector/composed roles. No low-resolution raster was introduced.

## Print-first status

`NOT_PRINT_READY` remains mandatory. Visual completion is not printer completion.

Still deferred:

- exact printer template and confirmed bleed/trim/safe;
- final imposition;
- CMYK/profile proof for deep navy and muted gold;
- 100% proof of gold hairlines and paper-field flecks/fibers;
- final menu/drink copy and real guest names;
- PDF export/preflight;
- physical or 100% print proof.

Raster PPI status: `N/A` for the newly adopted interior paper fields because they are vector-composed, not raster.

## Progression

Passport V4 now satisfies the reopened visual completion target and should not consume further hourly runs for cosmetic texture micro-polish. BOARDING PASS is already independently recorded as `SELLABLE_VISUAL_QA_PASS + DESIGN_QA_PASS_WITH_PLACEHOLDERS / NOT_PRINT_READY`; next safe clean-room progression target is 青春ふたりきっぷ.
