# ADD-03 当日タイムテーブルボード — Professional vNext TIDE DAY promotion

Date: 2026-08-21
State: `SELLABLE_VISUAL_QA_PASS + DESIGN_QA_PASS_WITH_PLACEHOLDERS / VNEXT_PRO_PROMOTED / LONG_COPY_STRESS_PASS / ROLLBACK_SAFE / NOT_PRINT_READY`
Start/latest authority SHA before evidence write: `3f00beb93f4531ed2e92177c6f3903ed2fea6143`
Current: `docs/automation/non-rurubu-figma-quality-current.md` = `ACTIVE / HOURLY / FIGMA_EDIT_ALLOWED / VISUAL_REOPENED`
Figma: `woFUHUqZcvNkih8o42xeH4`
Drive authority: `1uVcXv2Xs0H7juheHk977pt7YxLMJez_j / ADD-03_当日タイムテーブルボード`

## Clean-room brief

The new vNext pass was authored from blank frames using only confirmed facts and physical constraints. The prior selected `DAY INDEX / CHRONOGRAPHIC SHEET` visual was not used as an authoring source and was opened only after the new candidate family had passed its own visual/structure/stress checks.

Facts reused:

- A2 working size `1400×1980`;
- A3 working size `990×1400`;
- `2026.10.24 SAT / YOKOHAMA`;
- Ceremony `14:10–14:40`;
- unresolved interval `14:40–15:00` kept as native semantic guidance;
- Reception `15:00–17:30`;
- no invented opening time, room, floor, gate, flight number, QR or transport credential.

Emotional brief: **one continuous wedding day carried by tide / sunlight / movement rather than timetable cards or transport UI**.

## Three blank-frame directions

New page: `36:2 / VNEXT_PRO / ADD-03 / DAY OF JOURNEY / 2026-08-21`.

Three materially different A2 directions were authored from blank frames:

1. `36:3 / SUN PATH / DAYLIGHT SCORE`
   - warm cream daylight field with diagonal color sweeps;
   - clear but comparatively poster-minimal and less immersive.
2. `36:20 / TIDE / ONE CONTINUOUS DAY`
   - deep ocean field with large cyan/yellow/coral tide gestures;
   - strongest sense of movement, destination warmth, distance readability and celebration.
3. `36:40 / WIND CLOCK / BIG TYPE`
   - giant hour numerals plus vertical wind forms;
   - clean and legible but less emotionally rich and closer to a typographic event poster.

Professional review selected direction 2.

## Selected vNext family

Working clean-room masters:

- A2 `36:20 / VNEXT_SELECTED_CANDIDATE / ADD-03 A2 / TIDE DAY`;
- A3 `37:2 / VNEXT_SELECTED_CANDIDATE / ADD-03 A3 / TIDE DAY REFLOW`.

The A3 was authored as a fresh reflow at `990×1400`; it was not created by duplicating an old ADD-03 production/selected frame.

The hierarchy is Japanese-first and direct:

- `今日を、めぐる。`;
- date / Yokohama;
- large `14:10`, `14:40`, `15:00` chronology;
- `挙式 / 次の時間へ / 披露宴`;
- native editable unresolved/variable guidance;
- warm closing line.

No event cards, fake gate/class/flight data, barcode, scanner reticle, route nodes, shadow, gradient, generated person or stock airplane imagery was used.

## Screenshot-driven bounded repair

Initial full-size review showed the third coral tide field crossing the `—17:30` range role. That was repaired before selection by moving the fixed coral field down and keeping the time range in a clean navy lane.

Realistic long-copy stress was then created on:

- `37:22 / QA / ADD-03 VNEXT A2 / LONG COPY STRESS`;
- `37:42 / QA / ADD-03 VNEXT A3 / LONG COPY STRESS`.

Expanded native guidance revealed a second real defect: the cyan and coral fixed tide fields crossed the expanded right-side Ceremony/Reception copy lanes even though short placeholders looked fine.

The already-verified non-Rurubu QA method — revalidate fixed decoration against dynamic copy — was applied as a bounded repair. Only the cyan/coral tide field widths were shortened; typography, facts, time positions and semantic roles were unchanged. The same bounded geometry repair was applied to selected and stress roots.

Post-repair stress screenshots PASS: expanded Ceremony and Reception guidance stay on the deep-navy copy lane while the movement fields retain their visual role. Transfer guidance remains clear on the left and does not enter the yellow time field.

This is consumption of an existing shared QA method, not a new shared-learning claim, so no new feed entry was created.

## Hybrid authoring / asset decision

- factual and variable copy: native editable Figma text;
- fixed movement fields: editable Figma geometry;
- editable SVG roles: `0` required;
- generated raster assets: `0`;
- replaceable IMAGE roles: `0`;
- Drive writes: `0`.

`IMAGE_GENERATION_NOT_REQUIRED`: the live quality bottleneck was chronology, scale, movement and distance readability. Adding destination photography or generated illustration would have competed with the time information and increased stock/AI-template risk.

## Structure / long-copy QA

Before promotion:

- selected A2 `36:20`: `15/15` visible native text auto-height, IMAGE fills `0`, out-of-root visible text `0`;
- selected A3 `37:2`: `15/15` visible native text auto-height, IMAGE fills `0`, out-of-root visible text `0`;
- A2 stress `37:22`: `15/15` auto-height, outside visible text `0`;
- A3 stress `37:42`: `15/15` auto-height, outside visible text `0`.

Stress guidance bottoms remain inside their roots and visually clear fixed decoration after the bounded copy-lane repair. Stress proofs were hidden after verification.

## Mature-candidate comparison against retained selected family

Only after the new family was mature, the retained selected A2 `14:2` was opened for comparison.

The retained `DAY INDEX / CHRONOGRAPHIC SHEET` remains a competent, calm editorial schedule. The vNext TIDE DAY direction is materially better for the current explicit brief because it adds emotional movement, bright controlled color, stronger long-distance first read and a more memorable wedding-trip atmosphere without converting the timetable into cards or fake transport UI.

The prior selected roots were preserved before promotion on:

- `40:111 / ROLLBACK / ADD-03 / PRE-VNEXT-PROMOTION / 2026-08-21`.

## Professional Design Council score

Final vNext score: **92 / 100**.

- Concept clarity / ownability: `14 / 15`
- Emotional excitement / want-to-look-at-it: `15 / 15`
- Typography / Japanese editorial craft: `13 / 15`
- Composition / hierarchy / rhythm: `14 / 15`
- Travel / destination integration without cliché: `9 / 10`
- Timetable-specific function: `9 / 10`
- Physical print / viewing-distance credibility: `9 / 10`
- Editability / realistic-copy resilience: `5 / 5`
- A2/A3 family fit without template dependence: `4 / 5`

No Executive Creative Director, Japanese Editorial, or Print Production veto remains.

## Production/current selected promotion

The established selected root IDs were retained for downstream stability and received the mature vNext content after rollback preservation:

- `14:2` → `VNEXT_PRO_ADD03_TIDE_DAY_A2_SELECTED`;
- `15:40` → `VNEXT_PRO_ADD03_TIDE_DAY_A3_SELECTED`.

Post-promotion live A2 screenshot PASS.

Post-promotion structure readback:

- A2 `14:2`: `1400×1980`, `clipsContent=true`, `15/15` visible native texts auto-height, IMAGE fills `0`, outside visible text `0`;
- A3 `15:40`: `990×1400`, `clipsContent=true`, `15/15` visible native texts auto-height, IMAGE fills `0`, outside visible text `0`.

The older legacy production `1:5 / FRAME_TIMETABLE_BOARD` remains untouched.

## Decision

`SELLABLE_VISUAL_QA_PASS + DESIGN_QA_PASS_WITH_PLACEHOLDERS` for the new Professional vNext selected A2/A3 family.

Keep `NOT_PRINT_READY` until the final `14:40–15:00` wording, printer template/bleed/safe area, font/PDF embedding proof, physical A2/A3 proof, venue placement and viewing-distance checks are authoritative.

## Next target

Proceed to `ADD-04 受付サイン` with a new blank-frame Professional vNext direction. Do not reuse ADD-03's dark-ocean field, tide bands, alternating time layout or color geometry as a template; transfer only the quality bar and QA methods.