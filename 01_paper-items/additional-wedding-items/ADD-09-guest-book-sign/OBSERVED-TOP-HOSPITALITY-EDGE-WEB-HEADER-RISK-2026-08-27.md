# ADD-09 Guest Book — top hospitality edge visual reopen observation

Date: 2026-08-27
Start main: `740f11a6b2a74b618f0a5f104c249bfc6be099ef`
Current authority: `docs/automation/non-rurubu-figma-quality-current.md`
Scope: non-Rurubu only

## Status

`OBSERVED → ROOT_CAUSE_HYPOTHESIS / BOUNDED_FIGMA_TEST_PENDING`

This observation reopens only one fixed-art role. Existing structure, long-copy, Auto Layout, rollback, family-diversity and other verified evidence remain valid.

## Live authority recheck

- Figma file: `PjFWBpDwaQM5LfvgdqSFvU`
- Current: `41:56 / CURRENT / ADD-09 / PEN TRAY WELCOME / NO AMBIGUOUS PAPER EDGE / 2026-08-24`
- hidden long-copy stress: `41:76`
- exact Drive authority: `1D259ugx13El0JYxvn8yyskIjc2c2liF4 / ADD-09_ゲストブックサイン`

Drive folder metadata was live-confirmed again before this evidence write.

## Visible problem

Fresh whole-item / reading / native-size screenshots show the full-width deep hospitality-green strip at the top edge reading as a web/app header or status bar before it reads as a credible piece of paper/material structure.

The role is:

- `41:57 / TOP / HOSPITALITY EDGE`
- geometry: `x=0 / y=0 / w=1000 / h=54`

The Current already has a large dark-green writing-desk field at the bottom with a direct guest-action role, plus an open warm-paper editorial field and the connected pen cue. The top strip is visually detached from those physical roles.

At actual `1000×1419`, the top strip does not visibly express fold, binding, trim, thickness, paper overlap, information grouping or a reader-facing label. It therefore risks reintroducing the same containment grammar previously removed from other print items.

## Relevant shared-learning hypothesis

Consumed neutral method only:

`FULL_WIDTH_DECORATIVE_EDGE_READS_AS_WEB_HEADER` — already independently reproduced in ADD-06 and ADD-17.

This does **not** authorize automatic subtraction. ADD-10 and ADD-14 provide counterexamples where a top/full-width signal or color field has a real navigational or emotional job. ADD-09 still requires its own rollback-safe comparison.

No Rurubu item-specific production, asset, layout, palette or node was read or copied.

## Root-cause hypothesis

The defect is not the hospitality-green color itself. The likely cause is the combination of:

- full canvas width;
- shallow `54px` height;
- no contained label or guest-facing information;
- no visible material/binding relationship;
- position exactly on the canvas top edge.

That combination makes the strip behave like interface chrome rather than paper artifact structure.

## Exact bounded test for the next writable Figma run

Do **not** redesign the page and do not introduce a replacement decoration first.

Create rollback-safe duplicates of Current and long-copy proof, then compare only:

1. `CURRENT` — `41:57` retained;
2. `OPEN_PAPER_TOP` — only the copied `TOP / HOSPITALITY EDGE` hidden.

Keep unchanged:

- `GUEST BOOK` kicker;
- Japanese headline and lead;
- coral entry rule;
- dynamic Auto Layout info stack;
- closing/date;
- bottom writing desk;
- connected pen;
- all copy and placeholder wording;
- all current colors other than removing the top strip from the comparison;
- all sizes and coordinates.

Required QA:

- ~500px whole-item;
- ~1000px reading scale;
- native `1000×1419`;
- realistic long-copy proof;
- structure readback for native text / fixed-height / outside / IMAGE fills;
- confirm bottom desk and connected pen still provide enough physical writing-artifact identity.

Adopt subtraction only if the open-paper version is clearly stronger and does not make the top feel unfinished or weaken the hospitality/guest-book reading.

If Current is stronger, reject the hypothesis and close the reopen. Do not create a sequence of substitute top bars without new evidence.

## Hybrid authoring / image decision

No image generation is justified. The screenshot-supported defect is fixed-geometry containment, not missing photography, illustration, texture or hero art.

- native semantic/factual copy: unchanged
- current fixed desk / connected pen: unchanged
- editable SVG: not needed for this bounded test
- generated raster: `0`
- replaceable image role: `0`
- Drive write: `0`

## Current completion decision

Until the bounded comparison is executed, visual completion for ADD-09 should be treated as locally reopened:

`SELLABLE_VISUAL_QA_REOPENED + DESIGN_QA_PASS_WITH_PLACEHOLDERS / LOCAL_TOP_HOSPITALITY_EDGE_AUDIT_PENDING`

Do not invalidate the verified structural and long-copy evidence.
