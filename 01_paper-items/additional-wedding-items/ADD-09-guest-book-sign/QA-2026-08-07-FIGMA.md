# ADD-09 Figma production QA — 2026-08-09

Status: `DESIGN_QA_PASS_WITH_PLACEHOLDERS`

- Starting authority for this refinement run: `260af7b88b65805c221c907b961a2b95be3bf09b`
- Current authority checked immediately before write: `docs/automation/non-rurubu-figma-quality-current.md` = `ACTIVE / HOURLY / FIGMA_EDIT_ALLOWED`
- Figma production file key: `PjFWBpDwaQM5LfvgdqSFvU`
- Production frame: `1:3` (`FRAME_ADD09_GUEST_BOOK_SIGN`)
- Pre-edit rollback proof: `4:2` (`QA_ADD09_PRE_EDITORIAL_REFINEMENT_2026_08_09`)
- Long-copy stress proof: `4:14` (`QA_ADD09_LONG_COPY_STRESS_2026_08_09`)
- Primary size: A5 portrait proportional canvas, 1000 × 1419 px
- Drive authority folder: `ADD-09_ゲストブックサイン` / `1D259ugx13El0JYxvn8yyskIjc2c2liF4`
- Drive changes: none; the defect was composition/typography and no raster asset is required by SPEC.

## Highest-value visible defect

The initial production was structurally safe but still showed strong generic-template cues: a 240 px solid navy header block, oversized all-caps `GUEST BOOK` as the dominant title, a mint route line with two terminal dots, and a large hero-like empty composition. It read closer to a generated web/wayfinding template than a refined guest-book desk sign.

## Editorial refinement

A complete pre-edit rollback duplicate was created before production was changed. Production was then refined natively:

- reduced the 240 px navy header block to a restrained 10 px top edge;
- changed `GUEST BOOK` from an 82 px hero headline to a 20 px tracked secondary kicker;
- promoted the Japanese `ゲストブック` title to `Noto Serif JP Bold` at 58 px;
- changed the primary instruction placeholder to `Noto Serif JP Medium` at 30 px;
- reflowed the secondary operational placeholder below it at 24 px;
- converted the route line into a single 1 px editorial hairline;
- hid both route terminal dots non-destructively instead of deleting them;
- normalized the footer to `2026.10.24 · YOKOHAMA`;
- preserved `GUIDE_SAFE_10MM`, native text, semantic placeholders, and the original production dimensions.

No cards, badges, gradients, shadows, fake controls, fake transport data, or rasterized text were introduced.

## Screenshot QA

Post-edit whole-item / reading / detail screenshot QA was captured at the full 1000 × 1419 production size. The refined version has a clearer Japanese-first hierarchy, controlled asymmetry, quieter decorative density, and intentional negative space without the previous oversized hero/header treatment.

A dedicated long-copy proof tested:

- a two-line guest-book instruction placeholder;
- a longer writing-method / pen / placement operational placeholder.

Both wrapped inside the 856 px text measure in the stress screenshot without colliding with the hairline, title, footer, or frame boundary.

## Structure readback

Production remains 1000 × 1419 with `clipsContent=true` and exactly 5 native text nodes. Visible title/instruction text uses available fonts with `hasMissingFont=false`. The two route dots remain in the structure but `visible=false`, preserving rollback-safe subtraction. No flatten/raster replacement was introduced.

## Status

`DESIGN_QA_PASS_WITH_PLACEHOLDERS / WHOLE_READING_DETAIL_QA_PASS / LONG_COPY_STRESS_PASS / NATIVE_EDITABLE_PASS / SEMANTIC_PLACEHOLDERS_PASS / ROLLBACK_SAFE / DRIVE_AUTHORITY_VERIFIED / NOT_PRINT_READY`

## Deferred finalization

The following remain `DEFERRED_FINALIZATION` and do not block progression to ADD-10:

- final writing method / placement / pen operation;
- printer bleed/template/profile;
- 100% physical proof and venue-distance readability.
