name: Motion Studio CI

on:
  pull_request:
    paths:
      - "motion-studio/**"
      - ".github/workflows/motion-studio-ci.yml"
  push:
    paths:
      - "motion-studio/**"
      - ".github/workflows/motion-studio-ci.yml"

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    defaults:
      run:
        working-directory: motion-studio
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: motion-studio/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install ffmpeg for render QA
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq ffmpeg

      - name: Opening V1 media discovery
        run: pnpm prepare:opening-v1

      - name: TypeScript
        run: pnpm typecheck

      - name: Motion / asset / part / preset contracts
        run: pnpm check

      - name: Opening V1 sound contracts
        run: pnpm check:opening-sound

      - name: Opening V1 assembly preflight report stays honest on fresh clone
        run: |
          mkdir -p out/qa
          node --no-warnings scripts/opening-v1-assembly-preflight.mts --json > out/qa/opening-v1-assembly-preflight.json
          node -e "const fs=require('fs'); const r=JSON.parse(fs.readFileSync('out/qa/opening-v1-assembly-preflight.json','utf8')); if(r.schemaVersion!=='opening-v1-assembly-preflight/v1') process.exit(1); if(r.photos.expectedCount!==11) process.exit(1); if(r.readiness.finalRenderEligible!==false) process.exit(1); if(r.readiness.renderQaState!=='NOT_RUN') process.exit(1); if(r.readiness.macDaVinciActualState!=='NOT_RUN') process.exit(1);"
          if pnpm opening:assembly-preflight:strict; then
            echo "assembly preflight unexpectedly passed without real photos/BGM"
            exit 1
          fi
          if pnpm opening:assembly-preflight:mix-strict; then
            echo "mix-strict preflight unexpectedly passed without ambience"
            exit 1
          fi
          echo "assembly preflight correctly reports JSON and fails closed on missing real media"

      - name: Opening V1 preview review evidence starts NOT_RUN and fails closed
        run: |
          pnpm opening:preview-review
          if pnpm opening:preview-review:strict; then
            echo "preview review unexpectedly passed without human evidence"
            exit 1
          fi
          for token in "opening-v1-preview-review/v1" "STALE_PREVIEW_SHA256" "STALE_PHOTO" "STALE_BGM" "crop: 'NOT_RUN'" "focus: 'NOT_RUN'" "color: 'NOT_RUN'" "motion: 'NOT_RUN'"; do
            grep -q "$token" scripts/opening-v1-preview-review.mts || { echo "missing preview review contract token: $token"; exit 1; }
          done
          echo "preview review correctly remains NOT_RUN until a human reviews evidence bound to current media"

      - name: Opening V1 final render production bundle fails closed without approved media
        run: |
          if pnpm export:opening-v1-production-bundle; then
            echo "final render handoff unexpectedly exported without media/review/final render"
            exit 1
          fi
          for token in \
            "opening-v1-production-bundle/v1" \
            "FINAL_RENDER_BOUND_HANDOFF" \
            "finalRenderSha256" \
            "macActualState: 'NOT_RUN'" \
            "productionReady: false" \
            "RENDER_SHA_MISMATCH" \
            "opening-v1-production-bundle.json" \
            "opening-v1-palmier-timeline.csv"; do
            grep -q "$token" scripts/export-opening-v1-production-bundle.mts || { echo "missing final handoff contract token: $token"; exit 1; }
          done
          echo "final render handoff correctly stays blocked until current media, human preview review and final render all exist"

      - name: Opening V1 DaVinci finishing evidence starts NOT_RUN and fails closed
        run: |
          pnpm opening:davinci-finishing
          if pnpm opening:davinci-finishing:strict; then
            echo "DaVinci finishing evidence unexpectedly passed without Mac Actual"
            exit 1
          fi
          for token in \
            "opening-v1-davinci-finishing-evidence/v1" \
            "MAC_DAVINCI_ACTUAL_EVIDENCE" \
            "STALE_DAVINCI_FINISHING_BUNDLE_SHA" \
            "SOURCE_READBACK_SHA_MISMATCH" \
            "timelineInsertion: 'NOT_RUN'" \
            "playback1x: 'NOT_RUN'" \
            "watchedWithSound: 'NOT_RUN'" \
            "productionReady: false"; do
            grep -q "$token" scripts/opening-v1-davinci-finishing-evidence.mts || { echo "missing DaVinci finishing contract token: $token"; exit 1; }
          done
          echo "DaVinci finishing correctly remains NOT_RUN until bundle-bound Mac Resolve Actual is recorded"

      - name: Opening V1 final delivery approval remains human-only and fail-closed
        run: |
          pnpm opening:final-delivery-approval
          if pnpm opening:final-delivery-approval:strict; then
            echo "final delivery approval unexpectedly passed without bundle-bound human approval"
            exit 1
          fi
          for token in \
            "opening-v1-final-delivery-approval/v1" \
            "HUMAN_FINAL_DELIVERY_APPROVAL" \
            "decision: 'HOLD'" \
            "productionReady: false" \
            "STALE_FINAL_DELIVERY_BUNDLE_SHA" \
            "STALE_FINAL_DELIVERY_DAVINCI_EVIDENCE_SHA" \
            "FINAL_DELIVERY_DECISION_" \
            "productionReady=true is valid only"; do
            grep -q "$token" scripts/opening-v1-final-delivery-approval.mts || { echo "missing final approval contract token: $token"; exit 1; }
          done
          echo "final delivery approval cannot be manufactured by CI and only becomes production-ready after explicit SHA-bound human approval"

      - name: Opening V1 production status identifies the exact first blocker
        run: |
          mkdir -p out/qa
          node --no-warnings scripts/opening-v1-production-status.mts --json > out/qa/opening-v1-production-status.json
          node -e "const fs=require('fs'); const r=JSON.parse(fs.readFileSync('out/qa/opening-v1-production-status.json','utf8')); if(r.schemaVersion!=='opening-v1-production-status/v1') process.exit(1); if(r.authority!=='DERIVED_PRODUCTION_STATUS') process.exit(1); if(r.overallState!=='MEDIA_REQUIRED') process.exit(1); if(r.stages.media.state!=='BLOCKED') process.exit(1); if(r.stages.previewRender.state!=='NOT_RUN') process.exit(1); if(r.readiness.macDaVinciActualVerified!==false) process.exit(1); if(r.readiness.productionReady!==false) process.exit(1);"
          if pnpm opening:production-status:strict; then
            echo "production status unexpectedly reached DaVinci Actual on a fresh clone"
            exit 1
          fi
          for token in \
            "opening-v1-production-status/v1" \
            "MEDIA_REQUIRED" \
            "AWAITING_FINAL_DELIVERY_APPROVAL" \
            "PRODUCTION_BUNDLE_STALE" \
            "DAVINCI_ACTUAL_REQUIRED_OR_STALE" \
            "productionReady: false" \
            "CI_MUST_NOT_PROMOTE_MAC_GUI_ACTUAL"; do
            grep -q "$token" scripts/opening-v1-production-status.mts || { echo "missing production status contract token: $token"; exit 1; }
          done
          echo "production status correctly reports the first actionable blocker without trusting downstream artifacts"

      - name: Opening V1 final BGM gate fails closed without cleared media
        run: |
          if pnpm check:opening-sound:strict; then
            echo "final BGM gate unexpectedly passed on a fresh clone"
            exit 1
          fi
          echo "final BGM gate correctly blocks missing/unapproved BGM"

      - name: Opening V1 composition contract
        run: pnpm exec remotion compositions src/index-opening-v1.ts

      - name: StaRt Motion Kit composition contract
        run: pnpm exec remotion compositions src/index-start-motion-kit.ts

      - name: Opening V1 final-render QA contract
        run: |
          mkdir -p out/qa
          ffmpeg -hide_banner -loglevel error \
            -f lavfi -i "color=c=gray:s=640x360:r=30:d=1" \
            -c:v libx264 -pix_fmt yuv420p -y out/qa/render-contract.mp4
          node --no-warnings scripts/check-opening-render.mts \
            out/qa/render-contract.mp4 \
            --width=640 --height=360 --fps=30 --duration=1 --duration-tolerance=0.05

      - name: Opening V1 visual smoke stills
        run: node --no-warnings scripts/render-opening-v1-qa-stills.mts

      - name: Upload Opening V1 visual QA
        uses: actions/upload-artifact@v4
        with:
          name: opening-v1-qa-stills
          path: motion-studio/out/qa/opening-v1/*.png
          if-no-files-found: error
          retention-days: 7
